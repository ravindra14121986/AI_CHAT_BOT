# ServiceNow API connection settings.
# Keep this file private and do not commit it to source control.

[servicenow]
instance_url = https://dev272183.service-now.com/
username = admin
password = 1geEHg3fD*B!
timeout = 15

[api]
table = incident
limit = 100
